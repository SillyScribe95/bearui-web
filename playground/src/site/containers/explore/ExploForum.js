import React, {
  //
  useState,
  useContext,
} from "react";
import {
  //
  BearDiv,
} from "bearui-web";
import {
  //
  logs,
} from "@SillyScribe95/bedia-shared";

export function ExploForum({
  //
  seriesObj,
  ...args
}) {
  //
  // 1const
  const okfdsd = (
    <>
      {/*  */}
      ExploForum
    </>
  );

  args = {
    obj: okfdsd,
    ...args,
  };

  return <BearDiv {...args} />;
}
