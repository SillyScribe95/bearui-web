import React, {
  //
  useState,
  useContext,
} from "react";

import {
  //
  logs,
} from "@SillyScribe95/bedia-shared/";

export default function MediaFindChoose({
  //
  titleConfig,
  listConfig,
  ...args
}) {
  // 1const

  const okfdsd = (
    <>
      {/*  */}
      sssss
    </>
  );

  const isewease = {
    //
  };

  function ifjsqwas({ name }) {
    //
    return name;
  }

  const dfigjer = {
    //
    // typlist
    renderItem: ifjsqwas,
    ...args,
  };

  const sdkije = (
    <>
      CHOOSE MEDIA
      <BearList {...dfigjer} />
      {/* <ModelList {...dfigjer} /> */}
    </>
  );

  const sdifjew = {
    obj: sdkije,
  };

  return <BearDiv {...sdifjew} />;
}
