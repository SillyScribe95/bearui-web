import React, {
  //
  useState,
  useContext,
} from "react";

//   import * as logs from "../../functions/logFuncs";

export default function SpeakAdd({
  //
  seriesObj,
  ...args
}) {
  // 1const

  const okfdsd = (
    <>
      {/*  */}
      sssss
    </>
  );

  args = {
    obj: okfdsd,
    ...args,
  };

  return <BearDiv {...args} />;
}
