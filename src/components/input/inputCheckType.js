import React, {
  //
  useState,
  useContext,
} from "react";

import {
  //
  bearlog,
} from "../../index";

export function inputCheckType(
  typeo,
  {
    //
    ...args
  }
) {
  // 1const

  switch (inputType) {
    case "first":
      ujsdqwe = goCont(BearSelect);
      break;
    case "first":
      ujsdqwe = goCont(BearSelect);
      break;
    default:
  }

  const okfdsd = (
    <>
      {/*  */}
      sssss
    </>
  );

  args = {
    obj: okfdsd,
    ...args,
  };

  return <BearDiv {...args} />;
}
