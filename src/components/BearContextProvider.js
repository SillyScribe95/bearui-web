import React, {
  //
  useState,
  useEffect,
  useContext,
} from "react";
import {
  //
  logs,
  // filtModelDocs,
  // backendInit,
} from "@SillyScribe95/bedia-shared";
// import { ThemeProvider } from "@chakra-ui/core";

import { ProvideMain } from "./provider/ProvideMain";
import { ProvideAuth } from "./provider/ProvideAuth";

// import { ProvideMain } from "./provider/ProvideMain";

export function BearContextProvider({ ...dsfer }) {
  const sidjfewr = <ProvideMain {...dsfer} />;

  const jads =
    //
    sidjfewr;
  // ProvideAuth({
  //   obj: sidjfewr,
  //   ...dsfer,
  // });

  return jads;
}
